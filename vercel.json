{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": { "installCommand": "npm install --legacy-peer-deps" }
    }
  ],
  "rootDirectory": "src",
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
}