{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": { "installCommand": "npm install --legacy-peer-deps" }
    }
  ],
  "framework": "nextjs",
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
}